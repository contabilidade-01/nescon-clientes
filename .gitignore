# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.env
.env.*
!.env.example
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Sessões de agente (worktrees, estado local) — nunca versionar
.claude/

# Certificados mTLS da Cora — NUNCA versionar (chave privada de produção).
# Em produção usar CORA_CERT_BASE64/CORA_KEY_BASE64 na env; local usa arquivos
# ignorados aqui. Ver api/src/cora.js (loadCertificates).
api/certificates/
*.pem
*.key
